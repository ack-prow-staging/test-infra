apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- cluster-scoped-rbac.yaml
# Lets Argo CD apply the Role and RoleBinding objects inside the charts it manages.
# Separate from cluster-scoped-rbac.yaml because these are namespaced on purpose - see
# the header there.
- namespaced-rbac.yaml
